/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "assets.aceternity.com"], // Combine domains in a single array
  },
};

module.exports = nextConfig;
